# Vancouver Event Discovery Map

MVP project for discovering Vancouver events on a Leaflet map with a Next.js frontend and FastAPI backend.

See `agent.md` for the build spec and `DEPLOY.md` for run/deploy instructions.

